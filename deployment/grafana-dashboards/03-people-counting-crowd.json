{
  "id": null,
  "uid": "soca-counting-crowd",
  "title": "SOCA — People Counting & Crowd",
  "tags": ["soca", "counting", "crowd"],
  "timezone": "Asia/Jakarta",
  "schemaVersion": 38,
  "version": 1,
  "refresh": "1m",
  "time": { "from": "now-24h", "to": "now" },
  "timepicker": {
    "refresh_intervals": ["10s", "30s", "1m", "5m", "15m", "30m"]
  },
  "templating": {
    "list": [
      {
        "name": "datasource",
        "type": "datasource",
        "pluginId": "postgres",
        "label": "Data Source",
        "hide": 0,
        "refresh": 1,
        "current": {}
      },
      {
        "name": "edge",
        "type": "query",
        "label": "Edge",
        "datasource": { "type": "postgres", "uid": "${datasource}" },
        "query": "SELECT DISTINCT edge_name FROM app_alert WHERE edge_name <> '' ORDER BY 1",
        "multi": true,
        "includeAll": true,
        "allValue": ".*",
        "refresh": 2,
        "hide": 0,
        "current": {}
      },
      {
        "name": "camera",
        "type": "query",
        "label": "Camera",
        "datasource": { "type": "postgres", "uid": "${datasource}" },
        "query": "SELECT DISTINCT COALESCE(NULLIF(camera_name,''), camera_id) FROM app_alert ORDER BY 1",
        "multi": true,
        "includeAll": true,
        "allValue": ".*",
        "refresh": 2,
        "hide": 0,
        "current": {}
      }
    ]
  },
  "annotations": { "list": [] },
  "panels": [
    {
      "type": "timeseries",
      "title": "Crossings IN / OUT — Hourly Volume",
      "id": 1,
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "datasource": { "type": "postgres", "uid": "${datasource}" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${datasource}" },
          "rawSql": "SELECT\n    $__timeGroupAlias(timestamp, $__interval),\n    SUM(\n        COALESCE((crossing_counts -> rule.key ->> 'in')::integer, 0)\n    ) AS crossings_in,\n    SUM(\n        COALESCE((crossing_counts -> rule.key ->> 'out')::integer, 0)\n    ) AS crossings_out\nFROM app_alert a,\n     jsonb_object_keys(a.crossing_counts) rule\nWHERE $__timeFilter(a.timestamp)\n  AND a.alert_category = 'Counting'\nGROUP BY 1\nORDER BY 1",
          "format": "time_series",
          "refId": "A"
        }
      ],
      "options": {
        "tooltip": { "mode": "multi" },
        "legend": { "displayMode": "list", "placement": "bottom" }
      },
      "fieldConfig": {
        "defaults": {
          "custom": { "fillOpacity": 10, "lineWidth": 2, "showPoints": "never" }
        },
        "overrides": []
      }
    },
    {
      "type": "barchart",
      "title": "Cumulative Crossings by Camera and Rule",
      "id": 2,
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "datasource": { "type": "postgres", "uid": "${datasource}" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${datasource}" },
          "rawSql": "SELECT\n    COALESCE(NULLIF(a.camera_name, ''), a.camera_id) AS camera,\n    a.edge_name,\n    rule.key AS rule_name,\n    SUM((rule.value ->> 'in')::integer) AS total_in,\n    SUM((rule.value ->> 'out')::integer) AS total_out,\n    SUM((rule.value ->> 'in')::integer)\n      - SUM((rule.value ->> 'out')::integer) AS net_count\nFROM app_alert a,\n     jsonb_each(a.crossing_counts) rule\nWHERE $__timeFilter(a.timestamp)\n  AND a.alert_category = 'Counting'\nGROUP BY a.camera_id, a.camera_name, a.edge_name, rule.key\nORDER BY total_in DESC",
          "format": "table",
          "refId": "A"
        }
      ],
      "options": {
        "orientation": "auto",
        "legend": { "displayMode": "list", "placement": "bottom" }
      },
      "fieldConfig": { "defaults": {}, "overrides": [] }
    },
    {
      "type": "timeseries",
      "title": "Daily Entry/Exit Balance per Camera",
      "id": 3,
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 8 },
      "datasource": { "type": "postgres", "uid": "${datasource}" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${datasource}" },
          "rawSql": "SELECT\n    $__timeGroupAlias(a.timestamp, $__interval),\n    COALESCE(NULLIF(a.camera_name, ''), a.camera_id) AS camera,\n    SUM((rule.value ->> 'in')::integer) AS entries,\n    SUM((rule.value ->> 'out')::integer) AS exits,\n    SUM((rule.value ->> 'in')::integer)\n      - SUM((rule.value ->> 'out')::integer) AS occupancy_change\nFROM app_alert a,\n     jsonb_each(a.crossing_counts) rule\nWHERE $__timeFilter(a.timestamp)\n  AND a.alert_category = 'Counting'\nGROUP BY 1, a.camera_id, a.camera_name\nORDER BY 1",
          "format": "time_series",
          "refId": "A"
        }
      ],
      "options": {
        "tooltip": { "mode": "multi" },
        "legend": { "displayMode": "list", "placement": "bottom" }
      },
      "fieldConfig": {
        "defaults": {
          "custom": { "fillOpacity": 5, "lineWidth": 2, "showPoints": "never" }
        },
        "overrides": []
      }
    },
    {
      "type": "timeseries",
      "title": "Crowd — Daily Alerts and Peak Count",
      "id": 4,
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 16 },
      "datasource": { "type": "postgres", "uid": "${datasource}" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${datasource}" },
          "rawSql": "SELECT\n    $__timeGroupAlias(timestamp, $__interval),\n    COUNT(*) AS crowd_alerts,\n    MAX(crowd_count) AS peak_crowd,\n    ROUND(AVG(crowd_count), 1) AS avg_crowd\nFROM app_alert\nWHERE $__timeFilter(timestamp)\n  AND alert_category = 'Crowd'\nGROUP BY 1\nORDER BY 1",
          "format": "time_series",
          "refId": "A"
        }
      ],
      "options": {
        "tooltip": { "mode": "multi" },
        "legend": { "displayMode": "list", "placement": "bottom" }
      },
      "fieldConfig": {
        "defaults": {
          "custom": { "fillOpacity": 10, "lineWidth": 2, "showPoints": "never" }
        },
        "overrides": []
      }
    },
    {
      "type": "barchart",
      "title": "Peak Crowd by Camera",
      "id": 5,
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 16 },
      "datasource": { "type": "postgres", "uid": "${datasource}" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${datasource}" },
          "rawSql": "SELECT\n    COALESCE(NULLIF(camera_name, ''), camera_id) AS camera,\n    edge_name,\n    COUNT(*) AS crowd_alerts,\n    MAX(crowd_count) AS peak_crowd,\n    ROUND(AVG(crowd_count), 1) AS avg_crowd,\n    MIN(crowd_count) AS min_crowd\nFROM app_alert\nWHERE $__timeFilter(timestamp)\n  AND alert_category = 'Crowd'\nGROUP BY camera_id, camera_name, edge_name\nORDER BY peak_crowd DESC",
          "format": "table",
          "refId": "A"
        }
      ],
      "options": {
        "orientation": "horizontal",
        "legend": { "displayMode": "list", "placement": "bottom" }
      },
      "fieldConfig": { "defaults": {}, "overrides": [] }
    },
    {
      "type": "barchart",
      "title": "Hourly Crowd Distribution",
      "id": 6,
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 24 },
      "datasource": { "type": "postgres", "uid": "${datasource}" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${datasource}" },
          "rawSql": "SELECT\n    EXTRACT(HOUR FROM timestamp AT TIME ZONE 'Asia/Jakarta')::integer AS hour,\n    ROUND(AVG(crowd_count), 1) AS avg_crowd_count,\n    MAX(crowd_count) AS peak_crowd_count,\n    COUNT(*) AS alert_count\nFROM app_alert\nWHERE $__timeFilter(timestamp)\n  AND alert_category = 'Crowd'\nGROUP BY hour\nORDER BY hour",
          "format": "table",
          "refId": "A"
        }
      ],
      "options": {
        "orientation": "auto",
        "legend": { "displayMode": "list", "placement": "bottom" }
      },
      "fieldConfig": { "defaults": {}, "overrides": [] }
    },
    {
      "type": "timeseries",
      "title": "Crowd Density (15-Minute Buckets)",
      "id": 7,
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 24 },
      "datasource": { "type": "postgres", "uid": "${datasource}" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${datasource}" },
          "rawSql": "SELECT\n    date_trunc('hour', timestamp AT TIME ZONE 'Asia/Jakarta')\n      + (FLOOR(EXTRACT(MINUTE FROM timestamp) / 15) * INTERVAL '15 minutes') AS time,\n    COALESCE(NULLIF(camera_name, ''), camera_id) AS camera,\n    ROUND(AVG(crowd_count), 1) AS avg_crowd\nFROM app_alert\nWHERE $__timeFilter(timestamp)\n  AND alert_category = 'Crowd'\nGROUP BY 1, camera_id, camera_name\nORDER BY 1",
          "format": "time_series",
          "refId": "A"
        }
      ],
      "options": {
        "tooltip": { "mode": "multi" },
        "legend": { "displayMode": "list", "placement": "bottom" }
      },
      "fieldConfig": {
        "defaults": {
          "custom": { "fillOpacity": 10, "lineWidth": 2, "showPoints": "never" }
        },
        "overrides": []
      }
    },
    {
      "type": "table",
      "title": "Crowd Events Exceeding Threshold (≥10)",
      "id": 8,
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 32 },
      "datasource": { "type": "postgres", "uid": "${datasource}" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${datasource}" },
          "rawSql": "SELECT\n    (timestamp AT TIME ZONE 'Asia/Jakarta') AS local_time,\n    COALESCE(NULLIF(camera_name, ''), camera_id) AS camera,\n    edge_name,\n    crowd_count,\n    rule_name,\n    snapshot_message\nFROM app_alert\nWHERE $__timeFilter(timestamp)\n  AND alert_category = 'Crowd'\n  AND crowd_count >= 10\nORDER BY crowd_count DESC, timestamp DESC\nLIMIT 200",
          "format": "table",
          "refId": "A"
        }
      ],
      "options": {},
      "fieldConfig": {
        "defaults": {},
        "overrides": [
          {
            "matcher": { "id": "byName", "options": "crowd_count" },
            "properties": [
              {
                "id": "thresholds",
                "value": {
                  "mode": "absolute",
                  "steps": [
                    { "color": "yellow", "value": 10 },
                    { "color": "orange", "value": 20 },
                    { "color": "red", "value": 50 }
                  ]
                }
              },
              { "id": "custom.displayMode", "value": "color-background" }
            ]
          }
        ]
      }
    },
    {
      "type": "table",
      "title": "People Count Events List",
      "id": 9,
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 42 },
      "datasource": { "type": "postgres", "uid": "${datasource}" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "${datasource}" },
          "rawSql": "SELECT\n    (timestamp AT TIME ZONE 'Asia/Jakarta') AS local_time,\n    COALESCE(NULLIF(camera_name, ''), camera_id) AS camera,\n    edge_name,\n    rule_name,\n    detection_count,\n    snapshot_message\nFROM app_alert\nWHERE $__timeFilter(timestamp)\n  AND alert_category = 'Counting'\nORDER BY timestamp DESC\nLIMIT 500",
          "format": "table",
          "refId": "A"
        }
      ],
      "options": {},
      "fieldConfig": { "defaults": {}, "overrides": [] }
    }
  ]
}
